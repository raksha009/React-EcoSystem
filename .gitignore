.gitignore/
node_modules/

1. npm init -y
git init
public folder -> index.html

Add (JSX)ES6 SUpport
npm install --save-dev @babel/core @babel/cli @babel/preset-env @babel/preset-react


(babel/preset-env : helps convert ES6 into readable JS)
Create babel.rc file

Create SRC folder with App.css, App.js, Index.js
npm install react react-dom


npm install --save-dev webpack webpack-cli webpack-dev-server style-loader css-loader babel-loader
(Webpack helps convert ES6 and JSX into common JS)
Create webpack.config.js
Now, Run on browser: 
npx webpack-dev-server --mode development

Make changes in browser w/o physically refreshing
npm i --save-dev react-hot-loader
and make changes in App.js
and in package.json
=>      now, to RUN: npm run dev
                BUILT: npm run build (shows dist file ) : unneeded for now


To stop running: press Control+ C